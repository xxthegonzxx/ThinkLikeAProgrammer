# Exercise 7-2

Rewrite our studentCollection functions from Chapter 4 (*addRecord* and *averageRecord*) so that instead of directly implmeneting a linked list, you use a package from the Go Library.
